# The single dependency list for this project. environment.yml installs exactly this
# via pip; there is no second list to keep in sync.
#
# EVERYTHING comes from PyPI, deliberately. Taking the numeric stack from conda-forge
# while installing torch from pip loads two OpenMP runtimes into one process on macOS
# and aborts with "OMP: Error #15". Conda supplies only the interpreter.
#
# Loosely pinned: this file BUILDS an environment. `python scripts/capture_env.py`
# RECORDS one, with exact resolved versions, and that is what belongs beside results.

torch>=2.2              # MPS build; verify with scripts/check_openmp.py
numpy>=1.24
scipy>=1.11             # Spearman, bootstrap CIs for section 3.5 reporting
pandas>=2.0
openpyxl>=3.1           # reads Label.xlsx
opencv-python>=4.8      # decoding, patch crops, heatmap argmax
statsmodels>=0.14       # Poisson GLM with log-exposure offset (section 3.5)
matplotlib>=3.7
pytest>=7.4
